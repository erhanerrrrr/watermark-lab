# P0/P1 改进完成记录

完成时间：2026-09-04。

## P0

- 新增 `audit_formal_v1.py`，检查 121,440 条结果键、manifest 隔离、环境快照、统计 CSV
  和 Web 冻结数字；当前审计通过。
- 固化 formal-v1 的历史脏工作树事实与两套真实运行环境，不把旧运行伪装成当前 clean
  commit；正式 WAM 兼容环境提供单独安装脚本。
- 正式运行器不再只覆盖一个 `run_metadata.json`，每次运行同时保存不可变元数据文件。
- 新增 6,640 条独立 clean 正/负检测结果、阈值冻结、TPR/FPR/ROC-AUC 与置信区间。

## P1

- 新增 24 条 robustness-v2 攻击和 3 种攻击算子，局部攻击支持位置、长宽比和复制源。
- 完成 40 张固定 test 图、四模型、3,840 条验证和 2,000 次图像级 Bootstrap。
- Web 正式结果页展示检测误报和 robustness-v2，不再只展示正样本恢复率。
- 论文口径固定为“几何失配显著改善但存在误报与约 0.8–0.9 秒额外解码成本”，不使用
  “所有攻击全面提升”的表述。

P0/P1 完成不等于课程全部交付完成。论文、PPT、演示视频和最终 Release 仍属于 M5.3。
